# Demo Video

- `demo-my_avd.mp4` — запись с `emulator -avd my_avd`.
- Сценарий в видео: `recovered -> pending_lock -> locked -> recovered`.
- Для новой записи используйте `./scripts/record_demo_video.sh` после вывода приложения в исходное состояние и запуска `adb wait-for-device`.
